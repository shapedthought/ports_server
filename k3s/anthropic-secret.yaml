# anthropic-secret.yaml
# Create with real key: kubectl create secret generic veeam-ports-enrichment \
#   --from-literal=ANTHROPIC_API_KEY=sk-ant-... -n veeam-ports
# This manifest is a template — do NOT commit real keys.
apiVersion: v1
kind: Secret
metadata:
  name: veeam-ports-enrichment
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
