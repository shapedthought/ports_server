# voyage-secret.yaml
# Create with real key: kubectl create secret generic veeam-ports-voyage \
#   --from-literal=VOYAGE_API_KEY=pa-... -n veeam-ports
# This manifest is a template — do NOT commit real keys.
apiVersion: v1
kind: Secret
metadata:
  name: veeam-ports-voyage
type: Opaque
stringData:
  VOYAGE_API_KEY: "REPLACE_ME"
